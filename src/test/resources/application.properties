
greeting.message=halo
greeting.app=quarkus

# For testing
#quarkus.datasource.url=jdbc:h2:tcp://localhost/mem:test
#quarkus.datasource.driver=org.h2.Driver

# drop and create the database at startup (use `update` to only update the schema)
#quarkus.hibernate-orm.database.generation = drop-and-create

# datasource
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=hello_test
quarkus.datasource.password=hello_test
# when using testcontainers, the following properties will be supplied at runtime
# via DatabaseResource#start class
#quarkus.datasource.reactive.url=postgresql://localhost:5432/hello_test
#quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/hello

# Flyway minimal config properties
quarkus.flyway.locations=db/test_migration
quarkus.flyway.migrate-at-start=true